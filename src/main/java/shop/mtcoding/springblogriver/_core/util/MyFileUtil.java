package shop.mtcoding.springblogriver._core.util;

import shop.mtcoding.springblogriver._core.error.exception.Exception500;

import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.util.Base64;
import java.util.UUID;

public class MyFileUtil {

    public String write(String imgBase64) {
//        Base64.getMimeDecoder();
//        UUID uuid = UUID.randomUUID();
//        String originalFilename = file.getOriginalFilename();
//        String uuidFilename = uuid + "_" + originalFilename;
//        try {
//            Path imgFilePath = Paths.get(filePath + uuidFilename);
//            Files.write(imgFilePath, file.getBytes());
//        } catch (Exception e) {
//            throw new Exception500("파일 업로드 실패 : " + e.getMessage());
//        }
//        return uuidFilename;
        return null;
    }


}
